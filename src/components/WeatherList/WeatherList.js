import React from 'react';
import "./WeatherList.css";

const WeatherList = ({ data }) => {
  return (
    <div id="weatherList">
        {/* {
            data.list ? 
            data.list.map((d) => 
                <Weather />
            ) :
            <></>
        } */}
    </div>
  );
};

export default WeatherList;